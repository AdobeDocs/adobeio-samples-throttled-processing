{
  "name": "poc-throttled-external-api",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "@adobe/aio-sdk": "^2.3.0",
    "csvtojson": "^2.0.10",
    "json2csv": "^5.0.1",
    "node-fetch": "^2.6.0",
    "uuid": "^8.0.0"
  },
  "devDependencies": {
    "jest": "^24.9.0",
    "@openwhisk/wskdebug": "^1.2.0"
  },
  "scripts": {
    "test": "jest --passWithNoTests ./test",
    "e2e": "jest --collectCoverage=false --testRegex ./e2e"
  },
  "engines": {
    "node": "^10 || ^12 || ^14"
  }
}
